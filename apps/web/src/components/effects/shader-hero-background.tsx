import { ShaderBackground } from '@sajha/ui';

/** The home hero's animated mesh gradient (shared with the admin panel). */
export function ShaderHeroBackground() {
  return <ShaderBackground testId="hero" className="bg-ink-950" />;
}
